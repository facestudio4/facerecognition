Expected on-device face models:

- `blazeface.tflite`: face detector model
- `mobilefacenet.tflite`: face embedding model

Place the actual `.tflite` files in this folder. The Flutter asset bundle includes `assets/models/` so these files will be packaged when present.
